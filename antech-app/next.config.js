/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

    images: {
        domains: ['quomodothemes.website'],
        formats: ["image/webp"],
    },
}

module.exports = nextConfig
